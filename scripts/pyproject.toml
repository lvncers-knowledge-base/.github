[project]
name = "script"
version = "0.1.0"
description = "lvncers Knowledge Base Repositories Sync Setup"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
    "requests>=2.34.2",
]
